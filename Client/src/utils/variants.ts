import { cva, type VariantProps } from 'class-variance-authority'

/**
 * Export cva and VariantProps for component variant creation
 * Simplifies creating typed component variants with TypeScript support
 */
export const CVA = cva
export { type VariantProps }
